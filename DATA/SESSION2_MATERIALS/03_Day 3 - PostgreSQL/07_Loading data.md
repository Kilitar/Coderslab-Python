# Loading data

Type: Exercise

## Loading data

In the file create a database connection object. Next call the SQL queries below and use Python to display their results in the Jupyter notebook. Let the queries:

1. Display all products from the `Motorcycles` product line.
2. Display all products with warehouse stock above `1000` and the cost below `20`USD.
3. Display all orders (`orders` table) made in 2003.
4. Display all orders (`orders` table) made in 2003 and canceled.

The queries are identical as those from Exercise 1 in the previous section.
