# Relationships and Python

Type: Exercise

## Loading data

In the file create a database connection object. Next call the SQL queries below and use Python to display their results in the Jupyter notebook. Let the queries

1. Connect the `products` and `productlines` tables and display all of the information in them.
2. Join  the `customers` and `employees` tables and display all of the information in them.
3. They will display information about which employees are assigned to which office.

The queries are identical to those from Exercise 1 in this subject.
