# Sum of numbers

Type: Exercise

Write a program, that:

- calculates the sum of all numbers from `0` to `n`, where `n` is any natural number.

Solve the task in two ways:

1. Using a loop.
2. Using Python's built-in `range` and `sum` functions.
