# Modulo division

Type: Exercise

1. Create two variables and assign any numbers to them.
2. Calculate the remainder of the division (`modulo`) of these numbers and assign the result to the variable `modulo_result`.
3. Display the `modulo_result` variable.

> **Hint:**
> 
> If you don't fully understand the operation of the modulo operator, practice it with other numbers.
