# Adding data

Type: Exercise

Write a following query:

1. Add a new class and assign it the newly-added teacher (`main_teacher_id` column).
2. Add 5 new students, assigning them to the new class (`class_id` column). Try to do this with a single SQL query.
