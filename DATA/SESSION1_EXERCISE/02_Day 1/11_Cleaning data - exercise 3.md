# Cleaning data - exercise 3

Type: Exercise

1. During the presentation, we verified the occurrence of missing values, but we did not take all the columns into account. Expand the table to include the missing variables and check if there are missing values in them.
2. For the variables with missing values, perform value completion:

- instrumentalness - replace missing values with zeroes,
- mode - replace missing values with zeroes or ones. Put zero or one at random.

1. Looking at the questions we'll be answering in the analysis, decide which variable will be redundant in solving the problem. Remove it from the data set.

### Good luck!
