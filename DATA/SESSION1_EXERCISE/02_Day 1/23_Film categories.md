# Film categories

Type: Exercise

## Film categories

Using the `UNION` property of returning a set by default, modify the query below to return the categories of films (`category`) without repetitions (do not use the `DISTINCT` clause here):

``` 
SELECT * FROM sakila.nicer_but_slower_film_list
```
