# Update

Type: Exercise

## Update

There are three columns in the `tasks.city_country` table:

- `city`,
- `country_id`,
- `country`.

At this point, the `country` column is empty; write the appropriate query and populate it using `UPDATE`.

In addition, after executing your query, write another query that will check that this column has definitely been filled.
