# Rentals and stocks

Type: Exercise

## Rentals and stocks

Write a query that joins the `rental` and `inventory` tables and displays the following columns:

- inventory_id,
- rental_id,
- film_id.

Again, use `INNER JOIN`.
