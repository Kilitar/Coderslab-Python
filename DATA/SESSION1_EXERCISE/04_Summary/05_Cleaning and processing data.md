# Cleaning and processing data

Type: Exercise

1. Open the `cars_data.xlsx` file.
2. Combine the data to get a summary table in which we will have all the information about cars. Hint: You can use the `XLOOKUP()` function or Power Pivot.
3. Think about which variables (columns) will be unnecessary when analyzing the data. Remove the unnecessary columns.
4. In a separate tab (sheet), create a summary table that will show you if and what data gaps there are for each variable.
5. Clean the data:

- set the correct data type for each variable
- consider how to treat the variables where we have missing data. Do the cleanup in such a way that retains as many observations as possible (especially if the data is important from the perspective of being able to answer questions).
  
  E.g., if there are observations in the data that have many blank/unknown values, then we can remove them. In the case where the variable is relevant for further analysis, keep the variables and apply one of the methods used to determine empty values in the dataset

1. Add new variables to the set:

- the number of miles we're able to drive on one tank (range).
- The difference between the sales year (2019) and the model year

1. Save the results of your work.

### Good luck!
